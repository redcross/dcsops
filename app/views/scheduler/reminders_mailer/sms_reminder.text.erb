Reminder: You are scheduled as <%= item.shift.name %> on <%= item.date.strftime("%a, %b %d") %> from <%= item.local_start_time.strftime("%l:%M%p").strip %> to <%= item.local_end_time.strftime("%l:%M%p").strip %>.  Please be reachable if we need you.  Thank you!