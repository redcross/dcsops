= form_for @scheduler_flex_schedule do |f|
  - if @scheduler_flex_schedule.errors.any?
    #error_explanation
      %h2= "#{pluralize(@scheduler_flex_schedule.errors.count, "error")} prohibited this scheduler_flex_schedule from being saved:"
      %ul
        - @scheduler_flex_schedule.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :person
    = f.text_field :person
  .actions
    = f.submit 'Save'
