%h1 Disaster Dispatch

%h3 Dispatching Incidents
.dispatch-table{data: {refresh_name: 'dispatch', refresh: collection_path(partial: 'dispatch_table')}}
  =render 'dispatch_table'

%h3 Open Incidents

=edit_panel_html

- content_for :footer_js do
  :javascript
    $(function() {
      window.editPanelController = window.editPanelController || new IncidentEditPanelController();
      window.dispatchController = window.dispatchController || new DispatchController();
    })
  =region_pubnub_setup