%h1 Incidents Notification

%p 
  You are currently subscribed to the Disaster Operations Report.
  
%p
  Reports will be sent to
  %strong #{resource.person.email}.

-if show_select_frequency and resource.persisted?
  %p
    Send report:
    =editable_assignable_select resource, :frequency, url: url_for(action: :update)

%p
  = form_tag url_for(action: :destroy), method: :delete do
    =submit_tag 'Unsubscribe', class: 'btn btn-primary'