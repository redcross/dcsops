require "spec_helper"

describe Scheduler::SwapMailer do
  #describe "swap_invite" do
  #  let(:mail) { Scheduler::SwapMailer.swap_invite }
#
  #  it "renders the headers" do
  #    mail.subject.should eq("Swap invite")
  #    mail.to.should eq(["to@example.org"])
  #    mail.from.should eq(["from@example.com"])
  #  end
#
  #  it "renders the body" do
  #    mail.body.encoded.should match("Hi")
  #  end
  #end
#
  #describe "swap_notify" do
  #  let(:mail) { Scheduler::SwapMailer.swap_notify }
#
  #  it "renders the headers" do
  #    mail.subject.should eq("Swap notify")
  #    mail.to.should eq(["to@example.org"])
  #    mail.from.should eq(["from@example.com"])
  #  end
#
  #  it "renders the body" do
  #    mail.body.encoded.should match("Hi")
  #  end
  #end
#
  #describe "swap_confirmed" do
  #  let(:mail) { Scheduler::SwapMailer.swap_confirmed }
#
  #  it "renders the headers" do
  #    mail.subject.should eq("Swap confirmed")
  #    mail.to.should eq(["to@example.org"])
  #    mail.from.should eq(["from@example.com"])
  #  end
#
  #  it "renders the body" do
  #    mail.body.encoded.should match("Hi")
  #  end
  #end

end
